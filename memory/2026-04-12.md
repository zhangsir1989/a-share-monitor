
---

## 新增炸板个股和次新个股模块 + 字段映射修复 (2026-04-12 14:41-15:16 UTC)

### 用户需求

在首页新增两个股票监控模块：
1. **炸板个股** - 监控涨停后炸板的股票
2. **次新个股** - 监控新股次新股

### 布局设计 📐

**最终布局**：
- 第一行：涨停个股（100% 宽度）
- 第二行：跌停个股（50%）+ 强势个股（50%）
- 第三行：炸板个股（50%）+ 次新个股（50%）← 新增

### API 接口 🔗

| 模块 | API 接口 | 数据量（2026-04-10） |
|------|----------|---------------------|
| **炸板个股** | `/hslt/zbgc/{tradeDate}/{licence}` | 27 条 |
| **次新个股** | `/hslt/cxgc/{tradeDate}/{licence}` | 122 条 |

### 字段映射 📊

#### 炸板个股（11 列）

| 列名 | API 字段 | 转换逻辑 |
|------|----------|----------|
| 代码 | `dm` | - |
| 名称 | `mc` | - |
| 最新价 | `p` | toFixed(2) |
| 涨跌幅 | `zf` | toFixed(2) ✅ 修复：使用 zf 而非 zdf |
| 连板数 | `lbc` | null→0 ✅ 修复 |
| 首次封板 | `fbt` | 092502→09:25:02 |
| 最后封板 | `lbt` | null→'--' |
| 成交额 | `cje` | 元→万元 |
| 流通市值 | `lt` | 元→亿元 |
| 炸板次 | `zbc` | - |
| 行业 | `hy` | - |

#### 次新个股（11 列）

| 列名 | API 字段 | 转换逻辑 |
|------|----------|----------|
| 代码 | `dm` | - |
| 名称 | `mc` | - |
| 最新价 | `p` | toFixed(2) |
| 涨跌幅 | `zf` | toFixed(2) |
| 量比 | `lb` | null→'0.00' |
| 成交额 | `cje` | 元→万元 |
| 换手率 | `hs` | toFixed(2) |
| 是否新高 | `nh` | '是'→1, '否'→0 |
| 涨停统计 | `tj` | - |
| 流通市值 | `lt` | 元→亿元 |
| 行业 | `hy` | - |

### 问题修复 🔴

#### 1. 炸板个股涨跌幅字段错误

**问题**：使用了错误的 API 字段 `zdf` 而非 `zf`
- `zf`: 涨跌幅%（正确，如 8.83%）
- `zdf`: 其他涨幅指标（错误，如 1.32%）

**修复**：
```javascript
// 修复前
changePercent: item.zdf !== null ? item.zdf.toFixed(2) : '0.00'  // ❌ 1.32%

// 修复后
changePercent: item.zf !== null ? item.zf.toFixed(2) : '0.00'   // ✅ 8.83%
```

#### 2. 炸板个股连板数显示 None

**问题**：`lbc` 字段为 null 时显示为 JavaScript 的 `None` 字符串

**修复**：
```javascript
lbc: (item.lbc !== null && item.lbc !== undefined) ? item.lbc : 0
```

#### 3. 次新个股表格 colspan 错误

**问题**：表头 11 列，但加载状态 `colspan="10"` 只有 10 列

**修复**：`colspan="10"` → `colspan="11"`

### 文件修改

| 文件 | 修改内容 | 版本号 |
|------|----------|--------|
| `src/data-api.js` | 新增 `fetchBreakBoardStocks()` 和 `fetchNewBaseStocks()` 函数 | - |
| `src/data-api.js` | 修复炸板个股字段映射（zf→zdf, lbc null 处理） | - |
| `src/server.js` | 导入并调用新函数，添加到 marketData | - |
| `public/index.html` | 新增两个卡片模块（HTML 结构 + 表头 + 分页控件） | v=20260463 |
| `public/index.html` | 修复次新表格 colspan 10→11 | v=20260463 |
| `public/js/app.js` | 新增表格更新函数 + 配置初始化 + 事件绑定 | v=20260463 |

### Git 提交

- `341953b` - feat: 新增炸板个股和次新个股模块
- `25f21bf` - fix: 修复炸板个股和次新个股初始化配置
- `d3524d8` - fix: 修复炸板个股字段映射错误（zdf→zf, lbc null 处理）
- `bffc21b` - fix: 修复次新个股表格 colspan 错误（10→11）

### 数据汇总（2026-04-10）

| 模块 | 数量 | 列数 | 分页 |
|------|------|------|------|
| 涨停个股 | 59 只 | 11 列 | 6 页 |
| 跌停个股 | 8 只 | 10 列 | 1 页 |
| 强势个股 | 188 只 | 9 列 | 19 页 |
| **炸板个股** | **27 只** | **11 列** | **3 页** |
| **次新个股** | **122 只** | **11 列** | **13 页** |

### 验证要点

1. ✅ 炸板个股 27 条数据完整显示
2. ✅ 次新个股 122 条数据完整显示
3. ✅ 涨跌幅显示正确（炸板：8.83%、8.21% 等；次新：272.77%、167.08% 等）
4. ✅ 连板数显示 0（不是 None）
5. ✅ 封板时间格式正确（09:25:02）
6. ✅ 金额单位正确（成交额：万元，流通市值：亿元）
7. ✅ 表格列数对齐正确（colspan=11）
8. ✅ 分页功能正常
9. ✅ 日期选择器统一控制
10. ✅ **必须强制刷新页面**（Ctrl+F5）

### 注意事项

- 炸板个股的 `lbt`（最后封板）字段：炸板股票通常无最后封板，显示 '--'
- 次新个股的 `nh`（是否新高）字段：API 返回"是"/"否"，前端转换为"✓ 新高"/"-"
- 所有金额字段统一单位转换
- 行业字段：API 返回则显示，否则显示"--"
