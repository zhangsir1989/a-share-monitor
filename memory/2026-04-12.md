
---

## 二级侧边栏 header 添加折叠按钮 (2026-04-12 05:14 UTC)

### 用户反馈
- 二级菜单需要添加折叠按钮
- 位置在二级菜单 header 上（标题旁边）
- 功能同 1 级菜单上的折叠按钮

### 实现方案

#### 1. JS 功能 (sidebar.js)

**新增 `initSubSidebarHeaderCollapse()` 函数**:
```javascript
function initSubSidebarHeaderCollapse() {
  const subSidebar = document.getElementById('sub-sidebar');
  if (!subSidebar) return;
  
  const header = subSidebar.querySelector('.sub-sidebar-header');
  if (!header) return;
  
  let collapseBtn = document.getElementById('sub-sidebar-header-collapse-btn');
  
  if (!collapseBtn) {
    // 创建折叠按钮
    collapseBtn = document.createElement('button');
    collapseBtn.id = 'sub-sidebar-header-collapse-btn';
    collapseBtn.className = 'sub-sidebar-header-collapse-btn';
    collapseBtn.innerHTML = '《';
    collapseBtn.title = '折叠菜单';
    header.appendChild(collapseBtn);
    console.log('✅ 二级侧边栏 header 折叠按钮已创建');
  }
  
  if (collapseBtn) {
    collapseBtn.addEventListener('click', handleSubSidebarHeaderCollapse);
  }
}
```

**新增 `handleSubSidebarHeaderCollapse()` 处理函数**:
```javascript
function handleSubSidebarHeaderCollapse() {
  const subSidebar = document.getElementById('sub-sidebar');
  const collapseBtn = document.getElementById('sub-sidebar-header-collapse-btn');
  
  sidebarState.subCollapsed = !sidebarState.subCollapsed;
  
  if (sidebarState.subCollapsed) {
    subSidebar.classList.add('collapsed');
    collapseBtn.innerHTML = '》';
    collapseBtn.title = '展开菜单';
  } else {
    subSidebar.classList.remove('collapsed');
    collapseBtn.innerHTML = '《';
    collapseBtn.title = '折叠菜单';
  }
  
  updateLayoutClasses();
  saveSidebarState();
}
```

**在 `initSidebar()` 中调用**:
```javascript
initMainSidebarCollapse();
initSubSidebarCollapse();
initSubSidebarHeaderCollapse();  // ✅ 新增
```

#### 2. CSS 样式 (sidebar.css)

**修改 header 布局**:
```css
.sub-sidebar-header {
  padding: 16px;
  border-bottom: 1px solid var(--border-color);
  display: flex;
  align-items: center;
  justify-content: space-between;  /* 标题和按钮分开 */
}
```

**添加按钮样式**:
```css
.sub-sidebar-header-collapse-btn {
  background: transparent;
  border: none;
  cursor: pointer;
  color: var(--text-secondary);
  font-size: 18px;
  font-weight: bold;
  padding: 4px 8px;
  border-radius: 4px;
  transition: all 0.2s;
}

.sub-sidebar-header-collapse-btn:hover {
  background: var(--bg-primary);
  color: var(--accent-color);
}
```

### 文件修改
- `sidebar.js`: 添加 header 折叠按钮功能
- `sidebar.css`: 添加 header 折叠按钮样式
- `custom.html`, `positions.html` 等：更新版本号 v=20260426

### Git 提交
- `1abf2f1` - feat: 二级侧边栏 header 添加折叠按钮

### 效果

| 功能 | 描述 |
|------|------|
| 按钮位置 | 二级菜单 header 右侧，标题旁边 ✅ |
| 按钮图标 | 《 (折叠) / 》 (展开) ✅ |
| 点击功能 | 折叠/展开二级侧边栏 ✅ |
| 状态持久化 | 保存到 localStorage ✅ |
| 与主侧边栏一致 | 功能相同 ✅ |

### 访问地址
- 自选股：http://localhost:3000/custom
- 持仓：http://localhost:3000/positions

**测试步骤**:
1. 打开自选股页面
2. 查看二级菜单 header → 右侧显示折叠按钮《 ✅
3. 点击按钮 → 二级菜单折叠，按钮变为》 ✅
4. 再次点击 → 二级菜单展开，按钮变为《 ✅
5. 刷新页面 → 保持折叠状态 ✅
