
---

## 删除二级菜单折叠按钮 + 优化一级菜单按钮样式 (2026-04-12 06:15 UTC)

### 用户反馈
1. 删除二级侧边栏中间的折叠按钮（红圈中的《按钮）
2. 一级菜单收起按钮去掉圆形外框，只保留箭头样式，让视觉更简洁

### 修复方案

#### 1. 删除二级侧边栏折叠按钮

**CSS 删除**:
```css
/* 删除整个 .sub-sidebar-collapse-btn 样式块 */
```

**JS 删除**:
- `initSubSidebarCollapse()` 函数
- `handleSubSidebarCollapse()` 函数
- `updateSubSidebarButton()` 函数
- `updateSubSidebarHeaderButton()` 函数

**保留**: `initSubSidebarHeaderCollapse()` 和 `handleSubSidebarHeaderCollapse()`（header 上的折叠按钮）

#### 2. 优化一级菜单收起按钮样式

```css
/* 修改前 */
.sidebar-collapse-btn {
  width: 36px;
  height: 36px;
  background: #2f363d;
  border: 2px solid #444c56;
  border-radius: 50%;  /* 圆形外框 */
  box-shadow: 3px 3px 10px rgba(0,0,0,0.5);
}

/* 修改后 */
.sidebar-collapse-btn {
  background: transparent;  /* 透明背景 */
  border: none;             /* 无边框 */
  border-radius: 4px;       /* 小圆角 */
  padding: 4px 8px;
  box-shadow: none;         /* 无阴影 */
}

.sidebar-collapse-btn:hover {
  color: #1890ff;           /* 悬停蓝色 */
  background: rgba(255, 255, 255, 0.1);  /* 淡入背景 */
}
```

#### 3. 清理残留引用

**handleMainSidebarCollapse**:
```javascript
// 修改前
const subCollapseBtn = document.getElementById('sub-sidebar-collapse-btn');
if (subCollapseBtn) {
  subCollapseBtn.innerHTML = '《';
}

// 修改后
const headerCollapseBtn = document.getElementById('sub-sidebar-header-collapse-btn');
if (headerCollapseBtn) {
  headerCollapseBtn.innerHTML = '《';
}
```

**restoreSidebarState**:
```javascript
// 所有 subCollapseBtn 引用改为 headerCollapseBtn
```

### 文件修改
- `sidebar.css`: 删除二级按钮样式 + 优化一级按钮样式
- `sidebar.js`: 删除二级按钮功能 + 清理引用
- HTML 页面：更新版本号 v=20260432

### Git 提交
- `e99c426` - feat: 删除二级菜单折叠按钮 + 优化一级菜单按钮样式

### 效果对比

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 二级菜单中间按钮 | 显示《 ❌ | 删除 ✅ |
| 一级菜单按钮 | 圆形外框 ❌ | 仅箭头 ✅ |
| 背景 | 深色圆形 ❌ | 透明 ✅ |
| 边框 | 2px 圆框 ❌ | 无 ✅ |
| 悬停效果 | 缩放 + 变色 | 淡入背景 + 蓝色 ✅ |
| 视觉风格 | 复杂 ❌ | 简洁 ✅ |

### 访问地址
- 自选股：http://localhost:3000/custom
- 持仓：http://localhost:3000/positions

**测试步骤**:
1. 刷新页面（Ctrl+F5 或 Cmd+Shift+R）
2. 二级侧边栏中间无折叠按钮 ✅
3. 一级菜单按钮仅显示箭头《 ✅
4. 无圆形外框，背景透明 ✅
5. 悬停时显示淡入背景和蓝色 ✅
6. 点击按钮可正常折叠/展开 ✅
