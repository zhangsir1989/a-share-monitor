
---

## 首页增加涨停个股和跌停个股模块 (2026-04-12 08:02 UTC)

### 用户反馈
- 首页增加涨停个股模块和跌停个股模块
- 删除板块资金流和涨停板块模块

### 修改方案

#### 1. data-api.js - 新增 API 函数

```javascript
// 获取涨停个股数据（使用新浪 API）
async function fetchLimitUpStocks() {
  // 新浪财经 API 获取涨跌幅排名（降序）
  const url = 'https://vip.stock.finance.sina.com.cn/quotes_service/api/json_v2.php/Market_Center.getHQNodeData?page=1&num=80&sort=changepercent&asc=0&node=hs_a';
  
  // 筛选涨停股（涨跌幅 >= 9.5%）
  const limitUpStocks = data.filter(item => parseFloat(item.changepercent) >= 9.5);
  return limitUpStocks.slice(0, 50);
}

// 获取跌停个股数据（使用新浪 API）
async function fetchLimitDownStocks() {
  // 新浪财经 API 获取涨跌幅排名（升序）
  const url = 'https://vip.stock.finance.sina.com.cn/quotes_service/api/json_v2.php/Market_Center.getHQNodeData?page=1&num=80&sort=changepercent&asc=1&node=hs_a';
  
  // 筛选跌停股（涨跌幅 <= -9.5%）
  const limitDownStocks = data.filter(item => parseFloat(item.changepercent) <= -9.5);
  return limitDownStocks.slice(0, 50);
}
```

#### 2. server.js - 修改数据获取

```javascript
const [volume, highTurnover, limitUpStocks, limitDownStocks] = await Promise.all([
  fetchMarketVolume(),
  fetchHighTurnover(),
  fetchLimitUpStocks(),
  fetchLimitDownStocks()
]);

marketData = {
  volume,
  highTurnover,
  limitUpStocks,
  limitDownStocks,
  lastUpdate: new Date().toISOString()
};
```

#### 3. index.html - 修改页面结构

删除：
- 涨停板块卡片（🔥图标）
- 板块资金流卡片（💰图标）

新增：
- 涨停个股卡片（📈图标）
- 跌停个股卡片（📉图标）

#### 4. app.js - 修改前端逻辑

- 添加 `updateLimitUpStocksTable()` 函数
- 添加 `updateLimitDownStocksTable()` 函数
- 修改 `loadAllData()` 调用新函数
- 修改 `clearMarketData()` 清空新表格
- 修改元素引用和配置初始化
- 修改排序事件初始化

### 数据验证

| 项目 | 数量 | 数据源 |
|------|------|--------|
| 涨停个股 | 50 只 | 新浪 API ✅ |
| 跌停个股 | 13 只 | 新浪 API ✅ |
| 高换手率 | 50 只 | 新浪 API ✅ |
| 总成交额 | 23228.69 亿元 | MyData API ✅ |

### 文件修改
- `data-api.js`: 新增 API 函数
- `server.js`: 修改数据获取
- `index.html`: 修改页面结构 + 更新版本号 v=20260440
- `app.js`: 修改前端逻辑

### Git 提交
- `83b71c4` - feat: 首页增加涨停个股和跌停个股模块

### 访问地址
- 首页：http://localhost:3000/

**测试步骤**:
1. 刷新页面（Ctrl+F5 或 Cmd+Shift+R）
2. 查看涨停个股卡片（📈）
   - 显示 50 只涨停股（涨跌幅≥9.5%）
   - 点击可跳转个股详情页
3. 查看跌停个股卡片（📉）
   - 显示 13 只跌停股（涨跌幅≤-9.5%）
   - 点击可跳转个股详情页
4. 涨停板块和资金流模块已删除 ✅
