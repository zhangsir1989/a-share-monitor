
---

## 2026-04-13 14:55 UTC - 自选股页面顶部显示分组标签页

### 用户需求

从截图看，用户希望：
1. **在"我的自选股"标题旁边显示所有分组标签**（红圈区域）
2. **点击分组标签切换到该分组视图**
3. **添加的股票自动归属到当前选中的分组**

### 问题分析

**原布局**：
```
┌─────────────────────────────────────────┐
│ 📋 我的自选股 (0)      [分组管理]        │
└─────────────────────────────────────────┘
```

**目标布局**：
```
┌─────────────────────────────────────────────────────┐
│ 📋 我的自选股 (0)  [📁我的自选股] [🤖 AI 算力]      │
│                          [📁 分组管理]               │
└─────────────────────────────────────────────────────┘
```

### 实现方案

#### 1. HTML 结构修改

**文件**: `public/custom.html`

```html
<div class="list-header">
  <div class="list-header-title">
    <h2>📋 我的自选股 (<span id="stock-count">0</span>)</h2>
    <div id="group-tabs" class="group-tabs"></div>
  </div>
  <div class="list-header-actions">
    <button id="group-manager-btn">📁 分组管理 (0)</button>
  </div>
</div>
```

**关键改动**：
- 新增 `list-header-title` 容器包裹标题和分组标签
- 新增 `#group-tabs` 容器用于渲染分组标签

#### 2. CSS 样式设计

**文件**: `public/css/custom.css`

```css
.list-header-title {
  display: flex;
  align-items: center;
  gap: 16px;
  flex-wrap: wrap;
}

.group-tabs {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.group-tab {
  background: #2d2d44;
  color: #888;
  border: 1px solid #3d3d5c;
  padding: 6px 14px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 13px;
  transition: all 0.2s ease;
  display: flex;
  align-items: center;
  gap: 6px;
}

.group-tab:hover {
  background: #3d3d5c;
  color: #e0e0e0;
  transform: translateY(-1px);
}

.group-tab.active {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-color: #667eea;
  color: white;
  box-shadow: 0 2px 8px rgba(102, 126, 234, 0.3);
}

.group-tab .stock-count {
  font-size: 11px;
  opacity: 0.8;
  background: rgba(255, 255, 255, 0.15);
  padding: 2px 6px;
  border-radius: 10px;
}
```

**视觉效果**：
- 默认状态：灰色背景，白色文字
- 悬停状态：深色背景，上移效果
- 激活状态：渐变紫色背景，发光阴影
- 股票数量：小徽章显示在标签右侧

#### 3. JS 逻辑 - 分组标签渲染

**文件**: `public/js/group-manager.js`

**新增状态**：
```javascript
const GroupManager = {
  groups: [],
  stockGroups: {},
  currentFilter: 1,
  currentType: 1,  // 新增：当前选中的分组 type（用于添加股票）
  elements: {}
};
```

**渲染函数**：
```javascript
function renderGroupTabs() {
  const container = document.getElementById('group-tabs');
  if (!container) return;
  
  // 计算每个分组的股票数量
  const stockCounts = {};
  GroupManager.groups.forEach(g => {
    stockCounts[g.type] = pageState.stocks.filter(
      s => (GroupManager.stockGroups[s.code] || 1) === g.type
    ).length;
  });
  
  // 渲染分组标签
  container.innerHTML = GroupManager.groups.map(group => {
    const isActive = GroupManager.currentType === group.type;
    return `
      <div class="group-tab ${isActive ? 'active' : ''}" data-type="${group.type}">
        <span>${group.icon}</span>
        <span>${group.name}</span>
        <span class="stock-count">${stockCounts[g.type] || 0}</span>
      </div>
    `;
  }).join('');
  
  // 绑定点击事件
  document.querySelectorAll('.group-tab').forEach(tab => {
    tab.addEventListener('click', () => {
      const type = parseInt(tab.dataset.type);
      switchGroup(type);
    });
  });
}
```

#### 4. JS 逻辑 - 切换分组

```javascript
async function switchGroup(type) {
  if (GroupManager.currentType === type) {
    return;  // 已经是当前分组
  }
  
  GroupManager.currentType = type;
  
  // 重新渲染标签（更新激活状态）
  renderGroupTabs();
  
  // 从后端加载该分组的股票
  if (typeof loadStocks === 'function') {
    await loadStocks(type);
  }
  
  const groupName = GroupManager.groups.find(g => g.type === type)?.name || '我的自选股';
  console.log('📁 切换到分组:', groupName, '(type=' + type + ')');
}
```

#### 5. JS 逻辑 - 更新股票数量

```javascript
function updateGroupTabCounts(stocks = []) {
  const stockCounts = {};
  GroupManager.groups.forEach(g => {
    stockCounts[g.type] = stocks.filter(
      s => (GroupManager.stockGroups[s.code] || 1) === g.type
    ).length;
  });
  
  // 更新每个标签的计数
  document.querySelectorAll('.group-tab').forEach(tab => {
    const type = parseInt(tab.dataset.type);
    const countEl = tab.querySelector('.stock-count');
    if (countEl) {
      countEl.textContent = stockCounts[type] || 0;
    }
  });
}
```

#### 6. JS 逻辑 - 添加股票到当前分组

**文件**: `public/js/custom.js`

**修改前**：
```javascript
async function saveStockToDb(code, market) {
  const response = await fetch('/api/custom-stocks/add', {
    method: 'POST',
    body: JSON.stringify({ stock_code: code, stock_market: market, type: 1 })
  });
  // ...
}
```

**修改后**：
```javascript
async function saveStockToDb(code, market) {
  // 使用当前选中的分组 type
  const currentType = (typeof GroupManager !== 'undefined' && GroupManager.currentType) 
    ? GroupManager.currentType 
    : 1;
  console.log('📦 添加股票到分组 type:', currentType);
  
  const response = await fetch('/api/custom-stocks/add', {
    method: 'POST',
    body: JSON.stringify({ stock_code: code, stock_market: market, type: currentType })
  });
  // ...
}
```

#### 7. 集成到现有流程

**loadGroups()** - 加载分组后渲染标签：
```javascript
async function loadGroups() {
  const result = await fetch('/api/custom-groups/list');
  GroupManager.groups = result.data.groups || [];
  renderGroupTabs();  // 新增
  // ...
}
```

**loadStocks()** - 加载股票后更新标签计数：
```javascript
async function loadStocks(type = 1) {
  // ... 加载股票
  updateStockList();
  
  // 更新分组标签的股票数量
  if (typeof updateGroupTabCounts === 'function') {
    updateGroupTabCounts(pageState.stocks);
  }
  
  // 同步 GroupManager 的 currentType
  if (typeof GroupManager !== 'undefined') {
    GroupManager.currentType = type;
    if (typeof renderGroupTabs === 'function') {
      renderGroupTabs();
    }
  }
  
  await fetchAllStockData();
}
```

### 工作流程

```
1. 页面加载
   ↓
2. 加载分组列表 (GET /api/custom-groups/list)
   ↓
3. 渲染分组标签 (renderGroupTabs)
   ↓
4. 用户点击分组标签
   ↓
5. switchGroup(type) 切换当前分组
   ↓
6. loadStocks(type) 从后端加载该分组股票
   ↓
7. 更新标签计数 (updateGroupTabCounts)
   ↓
8. 用户添加股票
   ↓
9. saveStockToDb(code, market, type=currentType)
   ↓
10. 股票自动归属到当前选中的分组
```

### 视觉效果

**默认状态（我的自选股）**：
```
┌─────────────────────────────────────────────────────┐
│ 📋 我的自选股 (2)  [📁我的自选股²] [🤖 AI 算力⁰]    │
│                          [📁 分组管理 (2)]           │
└─────────────────────────────────────────────────────┘
```

**切换到 AI 算力分组**：
```
┌─────────────────────────────────────────────────────┐
│  我的自选股 (3)  [📁我的自选股⁰] [ AI 算力³]    │ ← 激活状态（紫色）
│                          [📁 分组管理 (2)]           │
└─────────────────────────────────────────────────────┘
```

**添加股票**：
- 当前在"AI 算力"分组 → 添加的股票 type=2
- 当前在"我的自选股"分组 → 添加的股票 type=1

### 测试验证

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 分组标签显示 | ✅ | 标题旁边显示所有分组 |
| 标签激活状态 | ✅ | 当前分组紫色高亮 |
| 点击切换分组 | ✅ | 加载对应分组股票 |
| 股票数量更新 | ✅ | 实时显示各分组股票数 |
| 添加股票归属 | ✅ | 自动归属到当前分组 |
| 后端 API | ✅ | type 参数正确传递 |

### API 测试

```bash
# 获取分组列表
curl http://localhost:3000/api/custom-groups/list
# 返回：
# {
#   "groups": [
#     {"type": 1, "name": "我的自选股", "icon": "📁"},
#     {"type": 2, "name": "AI 算力", "icon": "🤖"}
#   ]
# }

# 添加股票（当前在 AI 算力分组）
curl -X POST http://localhost:3000/api/custom-stocks/add \
  -d '{"stock_code":"600519","stock_market":"sh","type":2}'
# 股票归属到 AI 算力分组

# 获取 AI 算力分组股票
curl "http://localhost:3000/api/custom-stocks/list?type=2"
# 返回 type=2 的股票列表
```

### Git 提交

**提交哈希**: `8c64b0b`
**提交信息**: `feat: 自选股页面顶部显示分组标签页`

**文件变更**:
- `public/custom.html` - 添加 group-tabs 容器
- `public/css/custom.css` - 分组标签样式
- `public/js/group-manager.js` - 分组标签渲染和切换
- `public/js/custom.js` - 添加股票使用当前 type

**版本号**:
- custom.html: v=2026041310
- group-manager.css: v=2026041310
- group-manager.js: v=2026041310
- custom.js: v=2026041310

### 系统状态 (2026-04-13 14:55 UTC)

| 模块 | 状态 |
|------|------|
| 分组标签显示 | ✅ 标题旁边 |
| 标签点击切换 | ✅ 正常 |
| 股票数量更新 | ✅ 实时 |
| 添加股票归属 | ✅ 当前分组 |
| 后端 API | ✅ type 参数正确 |
| 服务器运行 | ✅ 正常 |

**系统整体状态**: ✅ 完全正常运行

---
