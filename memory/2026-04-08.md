---

### 个股详细页开发进度 (2026-04-08)

#### API对接情况
| 模块 | MyData API | 状态 |
|------|-----------|------|
| 基本数据 | `hsstock/real/time` + `hsrl/ssjy` | ✅ 已对接 |
| 五档盘口 | `hsstock/real/five` | ✅ 已对接 |
| 财务指标 | `hsstock/financial/pershareindex` | ✅ 已对接 |
| 资金流向 | `hsrl/zbjy` + 本地计算 | ✅ 已对接 |
| 逐笔成交 | `hsrl/zbjy` | ✅ 已对接 |
| 历史数据 | `hsstock/history` | ✅ 已对接 |

#### 颜色逻辑
- **买卖五档**: 价格 ≥ 昨收价 → 红色（涨），价格 < 昨收价 → 绿色（跌）
- **资金分析**: 主力净流入 ≥ 0 → 红色（+流入），主力净流入 < 0 → 绿色（-流出）
- **今年涨幅**: 涨红跌绿

#### Git提交记录
- `33c3c07` - feat: 个股详细页基本数据新增字段 + 对接MyData API
- `2d8f309` - feat: 完善基本数据对接（量比、今年涨幅）
- `67f3b5a` - fix: 修复买卖五档颜色、成交额、股本显示及刷新时间单位
- `63ff23e` - fix: 资金分析饼图添加横向滚动条

---

### UI显示修复 (2026-04-08 16:02 UTC)

**修复问题**:
1. **买卖五档颜色** - CSS添加 `.order-price.up/down` 样式，根据价格涨跌动态变化
2. **成交额显示** - 移除错误乘10000，API返回元级别
3. **市值显示** - 添加'元'后缀（如：2177.34亿元）
4. **股本显示** - 自动选择亿股/万股单位
5. **刷新时间** - >500ms显示秒单位（1s/3s/5s）

---

### 资金分析饼图滚动条 (2026-04-08 16:07 UTC)

**问题**: 饼图左边被遮挡，无法完整查看

**修复**:
- `.capital-flow` 添加 `overflow-x: auto` 横向滚动
- `.capital-main` 设置 `min-width: 280px` 防止内容压缩
- `.capital-pie-chart` 设置 `flex-shrink: 0` 保持饼图完整

**待完善**:
- 外盘/内盘 - MyData API 暂无此字段

---

### 自选股页面横向滚动条 (2026-04-08 16:13 UTC)

**问题**: 自选股卡片左边被遮挡，无法完整查看

**修复**:
- `.stock-list-section` 添加 `overflow-x: auto` 横向滚动
- `.stock-list` 设置 `min-width: fit-content` 防止压缩
- `.stock-card` 设置 `min-width: 700px` 保持完整显示
- 响应式调整：小屏幕下显示详细数据（不再隐藏）

**Git提交**: `9e7f670` - fix: 自选股页面添加横向滚动条

---

### 分时走势图功能 (2026-04-08 16:16 UTC)

**问题**: 个股详细页分时走势无任何显示

**原因**: 前端缺少分时图表渲染代码

**修复**:
1. 新增 `chart.js` - Canvas 绘制分时走势图
   - 价格曲线（红涨绿跌）
   - 成交量柱状图
   - 昨收价参考线
   - 网格和时间轴标签
2. `api.js` 添加 `getIntraday` 方法，调用 `/api/intraday/:code`
3. `main.js` 在 `loadAllData` 中加载分时数据
4. `index.html` 添加 Canvas 元素和引用 chart.js

**API端点**: `/api/intraday/:code` 返回完整分时数据（242条）

**Git提交**: `feat: 添加分时走势图功能`

---

### 侧边栏折叠功能 + 页面布局优化 (2026-04-08 16:23 UTC)

**问题**: 自选股页面左侧个股名称、表头被侧边栏挡住

**修复**:

#### 1. 页面布局优化
- `container`添加`padding-left: 460px`（240px主侧边栏 + 200px二级侧边栏 + 20px间距）
- 折叠状态动态调整：
  - 主侧边栏折叠：`padding-left: 280px`
  - 二级侧边栏折叠：`padding-left: 280px`
  - 双侧边栏都折叠：`padding-left: 80px`

#### 2. 侧边栏折叠功能
- sidebar.js在`updateLayoutClasses`中更新container类名
- 初始化时调用`updateLayoutClasses`确保布局正确
- 折叠按钮已存在：主侧边栏和二级侧边栏各一个

#### 3. 移动端适配
- 移动端`container`使用固定`padding-left: 20px`
- 侧边栏变为悬浮层，不影响主内容布局

#### 4. 内容区域优化
- 指数显示区域改为`flex`布局，自适应宽度
- 股票卡片最小宽度减小至600px

**CSS文件修改**: style.css, sidebar.css, custom.css
**JS文件修改**: sidebar.js

**Git提交**: `feat: 侧边栏折叠功能 + 页面布局优化`